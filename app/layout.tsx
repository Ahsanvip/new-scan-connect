import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Antigravity QR - Vehicle Emergency Contact System",
  description: "Get WhatsApp notifications when someone needs to contact you about your vehicle. Built for Pakistan.",
  keywords: "QR code, vehicle contact, Pakistan, WhatsApp, emergency contact",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
